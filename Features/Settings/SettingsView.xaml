<Window x:Class="SpeechAgent.Features.Settings.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeechAgent.Features.Settings"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:SpeechAgent.Features.Settings"
        d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"        
        Title="SettingsView" Height="450" Width="450"
        WindowStartupLocation="CenterOwner">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Voice Medic Settings" FontSize="20" VerticalAlignment="Center" Margin="10"/>
      <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10">
        <Button Margin="0,0,10,0" Command="{Binding SaveSettingsCommand}">
          <Button.Content>
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" />
              <TextBlock Text="저장" Margin="5,0,0,0"/>
            </StackPanel>
          </Button.Content>
        </Button>
        <Button Command="{Binding CloseCommand}">
          <Button.Content>
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="Close" Width="16" Height="16" />
              <TextBlock Text="닫기" Margin="5,0,0,0"/>
            </StackPanel>
          </Button.Content>
        </Button>
      </StackPanel>
    </Grid>
    <Grid Grid.Row="1">
      <StackPanel Margin="10">
        <TextBlock Text="연결 키" FontSize="14" Margin="0,0,0,5"/>
        <TextBox materialDesign:HintAssist.Hint="Enter Connect Key" Style="{StaticResource MaterialDesignOutlinedTextBox}" Text="{Binding ConnectKey, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>
        <TextBlock Text="앱 선택" FontSize="14" Margin="0,0,0,5"/>
        <ComboBox materialDesign:HintAssist.Hint="Select Option" Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  ItemsSource="{Binding Options}" 
                  DisplayMemberPath="Key" 
                  SelectedValuePath="Value" 
                  SelectedValue="{Binding AppName}" 
                  Margin="0,0,0,0"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
