<UserControl x:Class="SpeechAgent.Features.Settings.FindWin.Controls.ControlPosBox"
        x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
       xmlns:local="clr-namespace:SpeechAgent.Features.Settings.FindWin.Controls"
          mc:Ignorable="d" 
           d:DesignHeight="60" d:DesignWidth="400">
    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10" Background="WhiteSmoke">
      <StackPanel Orientation="Horizontal">
         <TextBlock Text="{Binding Title, ElementName=Root}" 
       Margin="0,0,10,0" 
             VerticalAlignment="Center" 
   FontWeight="SemiBold"/>
            <TextBlock Text="컨트롤:" 
  Margin="0,0,5,0" 
         VerticalAlignment="Center" 
       FontSize="12"/>
            <TextBlock Text="{Binding ControlType, ElementName=Root}" 
   Width="120" 
            Margin="0,0,10,0" 
         VerticalAlignment="Center" 
           FontSize="12" 
        Background="WhiteSmoke" 
           Padding="5"/>
    <TextBlock Text="인덱스:" 
      Margin="0,0,5,0" 
        VerticalAlignment="Center" 
      FontSize="12"/>
          <TextBlock Text="{Binding Index, ElementName=Root}" 
     Width="60" 
         VerticalAlignment="Center" 
          FontSize="12" 
   Background="WhiteSmoke" 
         Padding="5"/>
        </StackPanel>
    </Border>
</UserControl>
