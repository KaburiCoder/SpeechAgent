<UserControl
  x:Class="SpeechAgent.Controls.CustomTitleBar"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:SpeechAgent.Controls"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="50"
  d:DesignWidth="800"
  mc:Ignorable="d">

  <Border Background="#2196F3" CornerRadius="8,8,0,0">
    <Grid Height="36" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <!--  앱 아이콘  -->
      <materialDesign:PackIcon
        Grid.Column="0"
        Width="20"
        Height="20"
        Margin="10,0,6,0"
        VerticalAlignment="Center"
        Foreground="White"
        Kind="{Binding IconKind, RelativeSource={RelativeSource AncestorType=UserControl}}" />

      <!--  타이틀  -->
      <TextBlock
        Grid.Column="1"
        VerticalAlignment="Center"
        FontSize="14"
        FontWeight="Medium"
        Foreground="White"
        Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />

      <!--  닫기 버튼  -->
      <Button
        Grid.Column="2"
        Width="28"
        Height="28"
        Margin="0,0,6,0"
        Command="{Binding CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Foreground="White"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="닫기">
        <materialDesign:PackIcon
          Width="15"
          Height="15"
          Kind="Close" />
      </Button>
    </Grid>
  </Border>
</UserControl>
