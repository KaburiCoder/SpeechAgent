<UserControl
  x:Class="SpeechAgent.Features.Settings.FindWin.Controls.ControlSettingTextBoxes"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="80"
  d:DesignWidth="800"
  Loaded="UserControl_Loaded"
  mc:Ignorable="d">
  <Grid x:Name="MainGrid" Margin="0,0,0,6">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="10" />
      <ColumnDefinition Width="30" />
      <ColumnDefinition Width="10" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="10" />
      <ColumnDefinition Width="30" />
    </Grid.ColumnDefinitions>

    <!--  첫 번째 컨트롤 타입  -->
    <TextBox
      Grid.Column="0"
      materialDesign:HintAssist.Hint="{Binding FirstHint, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
      Text="{Binding FirstControlType, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}" />

    <!--  첫 번째 인덱스  -->
    <TextBox
      x:Name="FirstIndexTextBox"
      Grid.Column="2"
      materialDesign:HintAssist.Hint="Index"
      Text="{Binding FirstIndex, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}" />

    <!--  두 번째 컨트롤 타입 (정규식 등)  -->
    <TextBox
      Grid.Column="4"
      materialDesign:HintAssist.Hint="{Binding SecondHint, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
      Text="{Binding SecondControlType, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}" />

    <!--  두 번째 인덱스  -->
    <TextBox
      x:Name="SecondIndexTextBox"
      Grid.Column="6"
      materialDesign:HintAssist.Hint="Index"
      Text="{Binding SecondIndex, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}" />
  </Grid>
</UserControl>
